# poke-insights
Poke-insights is a learning-focused backend project where two independent services communicate asynchronously via a message queue. It uses the free PokéAPI as an external data source so we can focus on architecture, not auth tokens.
